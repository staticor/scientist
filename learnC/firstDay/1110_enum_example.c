#include <stdio.h>
#include <limits.h>
#include <ctype.h>


int main(){

	enum Weekday {Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday};

	enum Weekday today = Saturday;
	enum Weekday tomorrow = Sunday;

	

	// printf("\nupper a %c", upper(character_a));
    return 0;
}

